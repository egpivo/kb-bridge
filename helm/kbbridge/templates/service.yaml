apiVersion: v1
kind: Service
metadata:
  name: {{ include "kbbridge.fullname" . }}
  labels:
    {{- include "kbbridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kbbridge.selectorLabels" . | nindent 4 }}
